#NestedClasses
Implemented a program that has 3 nested classes: passengers, airplane and airport. Each one holds an array of the previous class, and a UI is implemented with a menu which allows CRUD operations along with many others for each class. 
